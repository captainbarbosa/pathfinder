<%= form_for @activity do |f| %>
  <%= f.hidden_field :trip_id %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :date %>
  <%= f.datetime :date %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.submit %>
 <% end %>
